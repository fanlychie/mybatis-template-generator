#foreach($column in $columns)
	public $column.type $str.getOrIs($column.type)$str.capitalize($column.name)() {
		return $column.name;
	}
	
	public void set$str.capitalize($column.name)($column.type $column.name) {
		this.$column.name = $column.name;
	}
	
#end